# Elysia API (Bun). Must match server/env.ts (SERVER_PORT / PORT).
SERVER_PORT=3030

# Optional: call the API on this origin instead of same-origin /api (skips Vite proxy).
# If unset, the dev app uses window.location.origin and Vite proxies /api to Bun.
# VITE_API_URL=http://127.0.0.1:3030
